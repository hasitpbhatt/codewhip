## Paper scaffold: "A Physiology of Agent Harnesses" (docs-only)

The frame: the LLM is the nervous system; **the harness is the physiology it can't live without** — metabolism (token budget), innate + adaptive immunity (policy/jail + decline-mining), homeostasis (retry/failover routing), memory consolidation (compaction), habituation (repeat memo), apoptosis (process-tree kill). The frame is not the contribution — the contribution is that each organ yields a **metric and a falsifiable law**. Prior-art search confirms: immune-only is partly occupied (AgentAntibody, arXiv 2026 — prompt-injection antibodies), bio-motif framing has near-neighbors (arXiv:2607.04240) but no metrics-driven version, and the allometric token-allocation law for subagents appears **completely open** (closest: BAMAS, arXiv:2504.02051 — neither measures a scaling exponent). Hook citation: the Foundation Agents survey (2025) explicitly notes agents "lack an equivalent of the autonomic nervous system."

### 1. Write `docs/moat/15-agent-physiology.md` (number adjusted to match existing convention in docs/moat/)

Sections:

1. **Thesis & hook** — autonomic-gap motivation citation; harness-as-physiology claim; the rule that makes it science: every analogy must bind to a mechanism in the code, a metric, and a falsifiable prediction. Analogies that can't are cut (audit chain → "self-verifying ledger" stays supporting cast, not headline).
2. **Mechanism → organism mapping table** with file:line anchors (read the files during execution to fill exact lines):
   - Compact ceiling `min(60k, 0.7 × contextWindow)` (src/index.ts, src/provider-registry.ts `contextWindow`) → metabolic ceiling / allostatic load; provider class = metabolic rate (8k relay vs 200k model — the fixed --free death)
   - Compaction with CJK-aware `weightedLength` (src/compact.ts) → sleep-dependent consolidation / synaptic downscaling (Tononi & Cirelli SHY) — hypothesis: an optimal consolidation *frequency* exists
   - Adaptive subagent budget allocation (src/tools/delegate_many.ts, commit 54f7839) → **Kleiber allometry**; open law: optimal split across n subagents is sublinear (α < 1) — first measurement
   - Repeat memo: REPEAT_NUDGE_AT=3, redact-before-store, clear on edit/write/bash (src/loop.ts) → habituation / dishabituation (Kandel); ablation: memo-off vs memo-on
   - Static policy: `normalizeForMatching`, `matchScriptBlock` (src/policy.ts), jail secret net (src/tools/jail.ts) → innate immunity (barriers, PRRs, negative selection); permission-ladder precedence → response thresholds; over-restriction = **autoimmunity** (legit-command denial rate)
   - Failover rotation + bounded retry ledgers + free-chain + /model switch (src/loop.ts, src/free-chain.ts) → homeostatic negative feedback; 429/auth signals as inflammation → chemotactic recruitment (rotation); metric: fault-storm survival rate
   - Decline-mining (deferred track) → adaptive immunity via danger-theory two-signal (Matzinger co-stimulation; Yang 2014 DTA) — differentiated from AgentAntibody: harness-permission learning from one-bit human declines, not injection antibodies
   - treeKill + timeouts (src/tools/background.ts) → apoptosis vs necrosis (contained death vs port leak); checkpoints (src/checkpoints.ts) → dauer dormancy; subagent read-only restriction → terminal cell differentiation (supporting cast)
3. **Prior-art landscape** with links + one-line positioning each (AgentAntibody; Biological Motifs 2607.04240; Foundation Agents survey; Cognitive Homeostasis arXiv:2606.18065; allostatic agents Khan 2024; LST position 2505.00018; BAMAS; Self-Resource Allocation 2504.02051; MAS scaling laws; classic AIS: Matzinger, Yang 2014, Forrest).
4. **Three contributions** (hypothesis / metric / experiment design / cost each):
   - **C1 — Physiological robustness metrics + stress-test**: compaction survival curves across context-window classes; provider-fault-injection survival rate; escape/autoimmune tradeoff curve. Position+empirical; grows into a harness benchmark.
   - **C2 — Kleiber test for subagent budgets** (empirical spine, opens fastest): equal-split vs `b_i ∝ B·w_i^α` exponent sweep on the existing eval harness + delegate_many.
   - **C3 — Danger-theory policy induction from one-bit declines** (flagship growth path; 3–6 months; needs mining harness + escape-rate telemetry; cite and differentiate AgentAntibody).
5. **Honest verdict** — consistent with torvalds-architecture-review.md: analogy alone ≠ paper; umbrella + one measured law (C2) = credible workshop paper; C3 is the path it grows into. No A* claim today.
6. **Venue map** — arXiv position → agents workshops (ICSE-NIER/SEIP), ICARIS (natural home for C3), AAAI agent workshops; benchmark path for C1.
7. **Reopen triggers** (binding): real eval numbers exist; 90-day frame decided; fault-injection + allocation-experiment telemetry built. Paper track stays CLOSED until then (per your scaffold-only ruling).

### 2. `docs/roadmap.md` — one line pointing at the scaffold (frame chosen, track closed until triggers met).

### 3. Gates + commit — lint/typecheck run once (no src/ changes expected to break anything), commit the docs following the established strategy-doc pattern; push only if you ask.

No src/ changes, no schema changes (00-convergence frozen schemas untouched).